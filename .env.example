# Copy this to .env.local and fill in your MySQL credentials
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=schooldb

# When running `next start` (production) or for getServerSideProps fetch base URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000
